<br />
<br />
<div class="container">
  <div class="row">
    <div class="col-md-12 grid-margin stretch-card">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title">Default form</h4>
          <p class="card-description">
            Form
          </p>
          <form class="forms-sample" method="post" action="/san-pham/chi-tiet/{{product._id}}" enctype="multipart/form-data">
            <div class="form-group">
              <label for="name">Name</label>
              <input
                value="{{product.name}}"
                type="text"
                class="form-control"
                name="name"
                placeholder="name"
              />
            </div>
            <div class="form-group">
              <label for="price">Price</label>
              <input
                value="{{product.price}}"
                type="number"
                class="form-control"
                name="price"
                placeholder="price"
              />
            </div>
            <div class="form-group">
              <label for="quantity">quantity</label>
              <input
                value="{{product.quantity}}"
                type="number"
                class="form-control"
                name="quantity"
                placeholder="quantity"
              />
            </div>
            <div class="form-group">
              <label for="category">Category</label>
              <select class="form-control" name="category">
                {{#each categories}}
                  {{#if this.isSelected}}
                  <option selected value="{{this._id}}">{{this.name}}</option>
                  {{else}}
                  <option value="{{this._id}}">{{this.name}}</option>
                  {{/if}}
                {{/each}}
              </select>
            </div>
            <div class="form-group">
                <input type="file" name="image" id="image-file" class="form-control" onchange="onChangeFile()">
            </div>
            <div class="form-group">
              <img class="product-img" id="image-view" src="{{product.image}}" alt="hình ảnh sản phẩm"/>
            </div>
            <button type="submit" class="btn btn-primary me-2">Submit</button>
            <button class="btn btn-light">Cancel</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>


  <script>

//upload hình lên server và nhận vể đường link của tấm hình
    const onChangeFile = () => {
        const file = document.getElementById('image-file').files[0];
        const reader = new FileReader();
        reader.onload = e => {
            document.getElementById('image-view').src = e.target.result;
        }
        reader.readAsDataURL(file);
    }
  </script>